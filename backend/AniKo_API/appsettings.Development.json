{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "ConnectionStrings": {
    "//": "Local development only. Points at the throwaway Docker container documented in backend/README-local-db.md — port 55432, not 5432, so it cannot collide with a Postgres someone already runs on the default port. These credentials guard nothing: the container is local, disposable, and holds only seeded demo data. Production never reads this key; there DATABASE_URL is injected by Render and takes precedence.",
    "DefaultConnection": "Host=localhost;Port=55432;Database=aniko;Username=aniko;Password=aniko_dev"
  },
  "Cors": {
    "//": "Vite's dev server. This REPLACES the production list rather than adding to it — .NET configuration overlays arrays by index, so a shorter list here leaves the longer one's tail in place. Both entries are spelled out so what a developer's browser is allowed to do is visible here, not inferred from two files at once.",
    "AllowedOrigins": [
      "http://localhost:5173",
      "https://aniko-agri.netlify.app"
    ]
  },
  "Seed": {
    "//": "Demo data on locally, matching the Render deployment, so a developer sees the same dashboard the deployed site shows. Phase D consumes this; in Phase C it only has to exist and be readable.",
    "Demo": true
  }
}
